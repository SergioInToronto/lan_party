/**
 * common.mjs — registers site-wide custom elements.
 * Import once per page. No logic lives here, only element registration:
 * add a new shared component -> add one import line, nothing else.
 */
import './nav.mjs';
import './login-modal.mjs';
import './prefs-modal.mjs';
